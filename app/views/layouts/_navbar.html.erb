<nav class='navbar navbar-default'>
   <ul class='nav navbar-nav'>
      <a class='navbar-brand' href="<%= subs_url %>">Postr</a>
      <li class='home'><%= link_to "Home", subs_url %></li>
      <% if current_user #change to is admin? %>
         <li class='add-sub'><%= link_to "Add New Sub", new_sub_url %></li>
         <li><%= link_to "User List", users_url, class: 'user-l' %></li>
      <% end %>
      <% if post && post.id && current_page?(post_url(post)) %>
         <li><%= link_to "Back to sub", sub_url(post.sub_id) %></li>
      <% end %>
   </ul>
   <ul class="nav navbar-nav" style='float: right'>
      <% if current_user %>
         <li><%= link_to "Log Out", session_url, method: :delete %></li>
         <li class='navbar-text'><%= "logged in as: #{current_user.username}" if current_user %></li>
      <% else %>
         <li class='log-in'><%= link_to "Log In", new_session_url %></li>
         <li class='sign-up'><%= link_to "Sign Up", new_user_url %></li>
      <% end %>
   </ul>
</nav>
