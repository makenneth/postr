<!DOCTYPE html>
<html>
<head>
  <title>Postr</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
   <nav class='navbar navbar-default'>
      <ul class='nav navbar-nav'>
         <a class='navbar-brand' href="<%= subs_url %>">Postr</a>
         <li><%= link_to "Home", subs_url %></li>
         <% if current_user #change to is admin? %>
            <li><%= link_to "Add New Sub", new_sub_url %></li>
            <li><%= link_to "User List", users_url %></li>
         <% end %>
      </ul>
      <ul class="nav navbar-nav" style='float: right'>
         <% if current_user %>
            <li><%= link_to "Log Out", session_url, method: :delete %></li>
            <li class='navbar-text'><%= "logged in as: #{current_user.username}" if current_user %></li>
         <% else %>
            <li><%= link_to "Log In", new_session_url %></li>
            <li><%= link_to "Sign Up", new_user_url %></li>
         <% end %>
      </ul>
   </nav>
  <div class='col-md-10 col-md-offset-1'>
     <% if flash[:errors] %>
        <% flash[:errors].each do |error| %>
           <div class="bg-danger notification">
              <p><%= error %></p>
           </div>
        <% end %>
     <% end %>
     <%= yield %>
  </div>
</body>
</html>
